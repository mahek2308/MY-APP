import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nanakshop.app',
  appName: 'nanakshop',
  webDir: 'out',
  server:{
    url: "https://nanak.netlify.app/"
  }
};

export default config;
